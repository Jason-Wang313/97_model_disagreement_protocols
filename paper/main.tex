\documentclass{article}
\usepackage{iclr2026_conference,times}
\input{math_commands.tex}
\usepackage{hyperref}
\usepackage{url}
\usepackage{booktabs}
\usepackage{graphicx}
\usepackage{microtype}
\raggedbottom
\title{Model Disagreement Protocols: A Negative ICLR-Main Evidence Audit}
\author{Anonymous Authors}

\begin{document}
\maketitle

\begin{abstract}
This archive tests whether model disagreement becomes useful for robotics when it is classified into action protocols: commit, probe, switch controller, abstain, or recover. We rebuilt the original draft into a paper-specific executable audit with five robot task families, seven disagreement families, five stress splits, nine methods, seven seeds, ablations, calibration and safety metrics, pairwise comparisons, failure cases, and stress sweeps. A 2026-06-15 continuation rerun reproduced the negative result. The proposed protocol improves useful-disagreement recall, but it does not beat failure-aware recovery on combined-stress task success (0.637 versus 0.701), does not improve over the safest baselines on unsafe commits, and is undermined by ablations that remove probing or the protocol cost model. The terminal decision is \texttt{KILL\_ARCHIVE}, not ICLR-main submission.
\end{abstract}

\section{Question}
The seed idea was that robot world-model disagreement should not be treated as one scalar uncertainty signal. Instead, a robot should infer whether disagreement is epistemic uncertainty, aleatoric sensor noise, model misspecification, out-of-distribution action, harmless multimodality, sensor corruption, or controller instability, then choose an action protocol. This is a plausible idea, but it sits in crowded territory: robust control, conformal risk filtering, diagnostic probing, embodied world models, and failure-aware recovery already address many of the same deployment failures \citep{prior1,prior3,prior6}.

\section{Audit Design}
We built and reran a deterministic local benchmark rather than reusing the previous generic scaffold. Each seed/task/family group samples latent risk, disagreement features, calibration noise, and protocol costs. Methods choose among commit, probe, switch controller, abstain, and recover. The evaluated methods are mean ensemble policy, variance-threshold abstention, conformal risk filter, robust MPC fallback, failure-aware RL recovery, WorldBench-style diagnostic probing, ensemble forecasting calibration, the proposed disagreement protocol, and an action oracle.

The benchmark covers five task families: bimanual manipulation failure detection, contact-rich pick-and-place, deformable object handling, mobile navigation under sensor noise, and legged locomotion disturbance. The hardest split is \texttt{combined\_disagreement\_stress}. Metrics include disagreement-family accuracy, useful-disagreement recall, noise false-alarm rate, calibration error, probe informativeness, task success, unsafe commit rate, unnecessary intervention, recovery success, protocol cost, and regret to an action oracle.

\section{Combined-Stress Results}
Table~\ref{tab:combined} is the submission-critical result. The proposed protocol is not the best non-oracle method on task success. Failure-aware recovery is stronger on the main closed-loop objective and regret, even though it has much lower disagreement-family accuracy.

\begin{table}[t]
\caption{Combined-stress metrics. Higher is better for success, type accuracy, and recall; lower is better for unsafe commits, false alarms, and regret.}
\label{tab:combined}
\centering
\resizebox{\textwidth}{!}{\input{../results/combined_stress_table.tex}}
\end{table}

\begin{figure}[t]
\centering
\includegraphics[width=0.95\textwidth]{../figures/disagreement_task_outcomes.png}
\caption{Closed-loop outcomes under combined stress. The proposed protocol is safer than naive mean control but does not beat failure-aware recovery or robust fallback on the main task-success objective.}
\label{fig:outcomes}
\end{figure}

\begin{figure}[t]
\centering
\includegraphics[width=0.95\textwidth]{../figures/disagreement_classification_quality.png}
\caption{The proposed method improves useful-disagreement recall, but that diagnostic advantage does not convert into the best closed-loop performance.}
\label{fig:diagnostics}
\end{figure}

\section{Ablations}
The ablation study is fatal for the mechanism. Removing the probe action, removing the protocol cost model, or using a recovery-only protocol matches or beats the full method on success or regret. This means the evidence does not isolate a positive contribution from the proposed type-to-protocol mechanism.

\begin{table}[t]
\caption{Combined-stress ablations. The full protocol is not uniquely supported.}
\label{tab:ablations}
\centering
\resizebox{\textwidth}{!}{\input{../results/ablation_table.tex}}
\end{table}

\section{Pairwise Gate}
The strongest gate was defined before the run: the proposed method had to beat the strongest non-oracle success baseline and the safest baseline, while surviving ablations. Table~\ref{tab:pairwise} shows why it fails. Against failure-aware recovery, proposed minus baseline task success is negative. Against robust fallback, regret is worse. Against conformal filtering, unsafe commits are a statistical tie rather than a win.

\begin{table}[t]
\caption{Paired seed/task/family comparisons used for the terminal decision.}
\label{tab:pairwise}
\centering
\resizebox{\textwidth}{!}{\input{../results/pairwise_decision_table.tex}}
\end{table}

\begin{figure}[t]
\centering
\includegraphics[width=0.88\textwidth]{../figures/disagreement_stress_sweep.png}
\caption{Stress sweep. Simpler recovery/fallback strategies remain competitive as combined stress increases.}
\label{fig:sweep}
\end{figure}

\section{Failure Cases}
The most common failure pattern is not catastrophic unsafe commitment. It is unnecessary or expensive intervention. In epistemic and sensor-corruption cases, the protocol often pays for diagnostic action but does not gain enough task success to offset the cost. This is precisely the kind of negative evidence that should stop an ICLR-main claim: the mechanism is plausible, measurable, and still not superior to simpler recovery.

\section{Limitations}
This audit is local and executable, not real hardware evidence. It does not include a trained robot policy, high-fidelity simulator, physical robot rollouts, or external benchmark comparison. Those limitations would prevent main-track readiness even if the local result were positive. Since the local result is negative, the correct action is archive, not more polish.

\section{Terminal Decision}
The terminal decision is \texttt{KILL\_ARCHIVE}. The paper should not be submitted to ICLR main. The rebuilt and rerun evidence shows that the type-to-protocol diagnostics are real but not closed-loop superior. A revival would require a new empirical project showing that the full type-to-protocol mechanism beats robust fallback and failure-aware recovery on real or high-fidelity robot tasks.

\begingroup
\raggedright
\bibliographystyle{iclr2026_conference}
\bibliography{references}
\endgroup

\end{document}
